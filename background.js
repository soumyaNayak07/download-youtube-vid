// chrome.runtime.onMessage.addListener(function(request, sender, sendResponse) {
//     if (request.action === 'download') {
//       chrome.tabs.sendMessage(sender.tab.id, { action: 'download' });
//     }
//   });
  